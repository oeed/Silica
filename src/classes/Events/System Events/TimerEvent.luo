class TimerEvent extends Event 

	static default eventType = "timer";
	
	property Number timer

	--[[
		@constructor
		@desc Creates a timer event from the arguments
		@param [number] time -- the ID of the timer
	]]
	function initialise( Number timer )
		self.timer = timer
	end

end