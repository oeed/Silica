class ExitedInterfaceMouseEvent extends Event 
	
	static default eventType = "interface_mouse_exited"
	
	property Number x
	property Number y
	default isSentToChildren = false

	function initialise( x, y )
		self.x = x
		self.y = y
	end

end