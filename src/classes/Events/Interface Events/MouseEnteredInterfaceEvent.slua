
class MouseEnteredInterfaceEvent extends Event {
	
	static = {
		eventType = "interface_mouse_entered";
	};

	x = Number;
	y = Number;
	isSentToChildren = Boolean( false );

}

function MouseEnteredInterfaceEvent:initialise( x, y )
	self.x = x
	self.y = y
end
