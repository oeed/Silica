class EnteredInterfaceMouseEvent extends Event 
	
	static default eventType = "interface_mouse_entered";

	property Number x;
	property Number y;
	default isSentToChildren = false


	function initialise( x, y )
		self.x = x
		self.y = y
	end

end