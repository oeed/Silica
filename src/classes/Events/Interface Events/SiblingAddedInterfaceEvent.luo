class SiblingAddedInterfaceEvent extends InterfaceEvent 

	static default eventType = "interface_sibling_added";
	
	property View view -- the new sibling that was added
	default isSentToChildren = false

	function initialise( View view )
		self.view = view
	end

end