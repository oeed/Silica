class TextChangedInterfaceEvent extends InterfaceEvent 
	
	static default eventType = "interface_text";
	
	property String text -- the new text value
	property String oldText -- the previous text value

	function initialise( String text, String oldText )
		self.text = text
		self.oldText = oldText
	end


end