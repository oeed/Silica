class SiblingRemovedInterfaceEvent extends InterfaceEvent 
	
	static default eventType = "interface_sibling_removed";
	
	property View view -- the new sibling that was removed
	default isSentToChildren = false

	function initialise( View view )
		self.view = view
	end

end