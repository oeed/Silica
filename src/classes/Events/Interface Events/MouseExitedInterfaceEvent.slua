
class MouseExitedInterfaceEvent extends Event {
	
	static = {
		eventType = "interface_mouse_exited";
	};

	x = Number;
	y = Number;
	isSentToChildren = Boolean( false );

}

function MouseExitedInterfaceEvent:initialise( x, y )
	self.x = x
	self.y = y
end

